# V1.6 补充调整任务 - 方案生成页客服引导优化

**任务编号**: ADJ-V1.6-009  
**优先级**: P1（优化体验）  
**分配给**: GLM  
**预计工时**: 0.5天  
**创建时间**: 2026-04-14

---

## 任务目标

在方案生成页面增加客服联系引导，推销商品折扣优惠，创造客户沟通机会。与商品库客服联系功能结合，复用已有组件。

---

## 调整方案

### 1. 页面顶部添加优惠引导条（新增）

**位置**: 方案生成页面顶部，标题下方

**展示内容**:
```jsx
<div className="mb-6 p-4 bg-gradient-to-r from-amber-50 to-orange-50 border border-amber-200 rounded-lg">
  <div className="flex items-center justify-between flex-wrap gap-3">
    <div className="flex items-center gap-3">
      <span className="text-2xl">💰</span>
      <div>
        <p className="font-bold text-amber-800">联系客服获取折扣优惠，最低5折起</p>
        <p className="text-sm text-amber-700">专业采购顾问为您定制最优方案</p>
      </div>
    </div>
    <button 
      onClick={() => setShowContactModal(true)}
      className="px-4 py-2 bg-amber-500 text-white font-medium rounded-lg hover:bg-amber-600 transition-colors"
    >
      📞 联系客服
    </button>
  </div>
</div>
```

**实现要求**:
- 使用渐变色背景（amber-50 到 orange-50）
- 左侧显示💰图标 + 优惠文案
- 右侧显示"联系客服"按钮
- 点击按钮弹出客服联系弹窗（复用 `ContactServiceModal`）

---

### 2. 无匹配商品时优化引导（修改现有）

**位置**: 商品匹配不足时的警告区域

**现有代码位置**: `src/App.jsx` 约第745行

**修改前**:
```jsx
{productListResult?.noMatchWarning && !isExampleMode && (
  <div className="max-w-4xl mx-auto mt-4 p-4 bg-amber-50 border border-amber-200 rounded-lg">
    <div className="flex items-start gap-3">
      <span className="text-xl mt-0.5">⚠️</span>
      <div className="flex-1">
        <p className="font-medium text-amber-800">当前商品库暂无完全匹配您需求的商品</p>
        <p className="text-sm text-amber-700 mt-1">
          建议调整预算或场景条件后重新生成
        </p>
      </div>
    </div>
  </div>
)}
```

**修改后**:
```jsx
{productListResult?.noMatchWarning && !isExampleMode && (
  <div className="max-w-4xl mx-auto mt-4 p-4 bg-amber-50 border border-amber-200 rounded-lg">
    <div className="flex items-start gap-3">
      <span className="text-xl mt-0.5">⚠️</span>
      <div className="flex-1">
        <p className="font-medium text-amber-800">当前商品库暂无完全匹配您需求的商品</p>
        <p className="text-sm text-amber-700 mt-1">
          建议：1. 调整预算或场景条件后重新生成  2. 联系客服获取专属优惠和定制服务
        </p>
        <div className="mt-3 flex gap-3">
          <button 
            onClick={handleGenerateSolution}
            className="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700"
          >
            重新生成
          </button>
          <button 
            onClick={() => setShowContactModal(true)}
            className="px-4 py-2 bg-amber-500 text-white text-sm font-medium rounded-lg hover:bg-amber-600"
          >
            📞 联系客服
          </button>
        </div>
      </div>
    </div>
  </div>
)}
```

**修改要点**:
- 优化文案，增加"联系客服获取专属优惠和定制服务"
- 添加两个按钮："重新生成"和"联系客服"
- 点击"联系客服"弹出客服联系弹窗

---

### 3. 客服联系弹窗（复用已有组件）

**复用组件**: `src/components/ContactServiceModal.jsx`

**使用方式**:
```jsx
// 在 App.jsx 中已有 showContactModal 状态
const [showContactModal, setShowContactModal] = useState(false);

// 弹窗组件
<ContactServiceModal 
  isOpen={showContactModal}
  onClose={() => setShowContactModal(false)}
/>
```

**注意**: 复用 ADJ-6 中已创建的 `ContactServiceModal` 组件，无需重新创建。

---

## 涉及文件

```
修改：src/App.jsx
  - 方案生成页面顶部添加优惠引导条
  - 优化无匹配商品时的引导区域
  - 确保 ContactServiceModal 正确引入和使用
```

---

## 验收标准

- [x] 方案生成页面顶部显示优惠引导条（💰 联系客服获取折扣优惠，最低5折起）
- [x] 引导条样式美观，使用渐变色背景
- [x] 点击"联系客服"按钮弹出客服联系弹窗
- [x] 无匹配商品时显示优化后的引导文案
- [x] 无匹配商品区域显示"重新生成"和"联系客服"两个按钮
- [x] 客服联系弹窗正常显示（复用已有组件）
- [x] 不破坏现有方案生成逻辑
- [x] 不破坏现有商品库和客服联系功能

---

## 测试要点

1. 打开方案生成页面，确认顶部显示优惠引导条
2. 点击引导条的"联系客服"按钮，确认弹出客服联系弹窗
3. 输入条件生成方案，确认引导条不影响正常功能
4. 测试无匹配商品场景（如预算过低），确认优化后的引导显示正常
5. 点击无匹配区域的"联系客服"按钮，确认弹出客服联系弹窗
6. 确认"重新生成"按钮正常工作

---

## 输出要求

1. **代码提交**: `git commit -m "feat: V1.6 ADJ-9 方案生成页增加客服优惠引导"`
2. **自测报告**: 简要说明测试结果
3. **截图**: 如有条件，提供引导条和弹窗的截图

---

## 注意事项

- **不要破坏现有逻辑**：确保方案生成、商品匹配等核心功能正常工作
- **复用已有组件**：使用 ADJ-6 中已创建的 `ContactServiceModal`，不要重复创建
- **样式一致性**：引导条样式与页面整体风格保持一致
- **响应式**：确保移动端显示正常

---

## 关联任务

- **ADJ-6**: 商品库策略调整（已创建 `ContactServiceModal` 组件）
- **ADJ-8**: 修复重新生成按钮（确保重新生成功能正常）
